
<%= render @coverage %>

<div>
  <%= link_to "Edit", edit_coverage_path(@coverage), class: 'btn btn-primary' %> |
  <%= link_to "Back", coverages_path, class: 'btn btn-primary' %>

  <%= button_to "Delete", @coverage, method: :delete, class: 'btn btn-danger' %>
</div>
