
<div class="card">
  <h5 class="card-header">New dependent</h5>
  <div class="card-body">
    <%# render "form", dependent: @dependent %>
    <%= render "form", url: dependents_path(m: @member), meth: 'post' %>
  </div>
</div>
